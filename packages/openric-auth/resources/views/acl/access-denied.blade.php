@extends('theme::layouts.1col')
@section('title', 'Access Denied')
@section('content')
<div class="row justify-content-center"><div class="col-md-8"><div class="card border-danger shadow"><div class="card-header bg-danger text-white"><h4 class="mb-0"><i class="bi bi-lock me-2"></i>Access Denied</h4></div><div class="card-body"><p class="lead">You do not have permission to access:</p><p class="h5 text-muted">{{ e($objectTitle ?? 'this resource') }}</p><hr>@if(in_array('classification', $access['reasons'] ?? []))<div class="alert alert-warning"><h5><i class="bi bi-shield-lock me-2"></i>Security Classification Required</h5><p class="mb-0">This material is classified. @guest Please <a href="{{ route('login') }}">log in</a> to verify your clearance. @else Your current clearance does not permit access. @endguest</p></div>@endif @if(in_array('donor_restriction', $access['reasons'] ?? []))<div class="alert alert-info"><h5><i class="bi bi-person-lock me-2"></i>Donor Restriction</h5><p class="mb-0">Access restricted by donor agreement.</p></div>@endif<hr><div class="d-flex justify-content-between"><a href="javascript:history.back()" class="btn btn-outline-secondary"><i class="bi bi-arrow-left me-1"></i>Go Back</a>@guest<a href="{{ route('login') }}" class="btn btn-primary"><i class="bi bi-box-arrow-in-right me-1"></i>Log In</a>@else<a href="{{ route('security.my-requests') }}" class="btn btn-primary"><i class="bi bi-hand-index me-1"></i>Request Access</a>@endguest</div></div></div></div></div>
@endsection
