@extends('theme::layouts.1col')
@section('title', 'Set Up Two-Factor Authentication')
@section('content')
<div class="row justify-content-center mt-5"><div class="col-md-7"><div class="card"><div class="card-header bg-primary text-white"><h4 class="mb-0"><i class="bi bi-phone"></i> Set Up Two-Factor Authentication</h4></div><div class="card-body"><div class="alert alert-info">Your security clearance requires two-factor authentication.</div><h5>Step 1: Scan the QR Code</h5><p class="text-muted">Open your authenticator app and scan this QR code:</p><div class="text-center my-4">@if($qrCodeUrl ?? null)<img src="{{ e($qrCodeUrl) }}" alt="QR Code" class="border rounded p-2" style="max-width:220px;">@else<p class="text-muted">QR code will appear here.</p>@endif</div><div class="mb-4"><h6>Or enter this key manually:</h6><div class="input-group"><input type="text" class="form-control font-monospace text-center" value="{{ e($secret ?? '') }}" readonly></div></div><hr><h5>Step 2: Enter Verification Code</h5><form action="{{ route('acl.setup-2fa-store') }}" method="post">@csrf<div class="row justify-content-center"><div class="col-md-6"><div class="mb-3"><input type="text" name="code" class="form-control form-control-lg text-center" maxlength="6" pattern="\d{6}" placeholder="000000" required autocomplete="one-time-code" inputmode="numeric"></div><div class="d-grid"><button type="submit" class="btn btn-success btn-lg">Confirm & Activate</button></div></div></div></form></div></div><div class="text-center mt-3"><a href="{{ url('/') }}" class="text-muted">Cancel</a></div></div></div>
@endsection
